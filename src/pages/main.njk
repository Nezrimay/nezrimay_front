---
title: 'Незримая надстройка'
permalink: index.html
layout: page.njk
---

{% include 'header.njk' %}

<main class="main-page">
  <h1 class="visually-hidden">Незримая надстройка</h1>

  <section class="best-pages">
    <h2 class="best-pages__title grotesk grotesk-h1">Лучшие материалы</h2>
    <ul class="best-pages__list">
      {% for page in best %}
        <li class="best-pages__item">
          <div class="pages">
            <div class="pages__wrapper-img">
              <img class="pages__img" width="250" height="250" src="{{page.image}}"/>
            </div>
            <p class="pages__title grotesk grotesk-nav">{{ page.title }}</p>
            <p class="pages__date grotesk grotesk_secondary grotesk-b2">{{ page.date }}</p>
          </div>
        </li>
      {%endfor%}
    </ul>
  </section>

  <section class="last-pages">
    <h2 class="last-pages__title visually-hidden">Последние материалы</h2>
    <ul class="last-pages__list">
      {% for page in pages %}
        <li class="last-pages__item">
          <div class="pages">
            <div class="pages__tags">{{ page.tags }}</div>
            <div class="pages__wrapper-img">
              <img class="pages__img pages__img_full" width="100%" src="{{page.image}}"/>
            </div>
            <p class="pages__title grotesk grotesk-nav">{{ page.title }}</p>
            <p class="pages__date grotesk grotesk_secondary grotesk-b2">{{ page.date }}</p>
          </div>
        </li>
      {%endfor%}
    </ul> 
  </section>

  <section class="about">
    <h2 class="about__title">Незримая Надстройка</h2>
    <p class="about__subtitle">Переоткрытие сокровищ домодерного Крыма, вселенные в форме кенас, вымышленные этюды Мешкова, покраска Черного моря, трансисторизм и бойсовские татары</p>
  </section>

  {{content | safe}}
</main>

{% include 'footer.njk' %}